fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.36
pymysql==1.1.1
cryptography==44.0.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-dotenv
PyJWT==2.10.1
httpx>=0.27.0
captcha>=0.7.1
imageio-ffmpeg>=0.6.0
PyYAML>=6.0

# 数据统计研判模块
APScheduler>=3.10.0

# 视频识别分析模块
# M2 目标检测采用纯 ONNX 链路（onnxruntime + opencv），避免引入 ultralytics/PyTorch 的 ~2GB 体积。
opencv-python-headless>=4.8   # 帧解码 / 标注渲染
numpy>=1.24                   # 数组操作
onnxruntime>=1.16.0           # ONNX 推理引擎（CPU 通用）
# ultralytics>=8.1.0            # YOLOv8（仅在有 GPU/强 CPU 时选用）
